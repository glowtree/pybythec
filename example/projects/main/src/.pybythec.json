
{
  /// optional as you can supply this as parameters to the build function
  "compiler": "gcc",      // gcc-4.4 gcc msvc110
  "osType":   "linux",    // linux osx windows android ios
  "buildType": "release", // debug release
  
  "target": "main",
  "binaryType" : "executable",
  
  "sources": "main.cpp",
  "libs": ["StaticLib", "DynamicLib"], /// TODO: the .so /.dll of DynamicLib has to be in PATH, LD_LIBRARY_PATH directory or next to the executable
  
  "linkFlags": {
    "msvc": "-SUBSYSTEM:CONSOLE"
  },
  
  // paths
  "incPaths":    "../../../shared/include",
  "libPaths":    "../../../shared/lib",
  "libSrcPaths": "../../../shared/src",
  "installPath": ".."
}
